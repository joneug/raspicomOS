#!/usr/bin/env bash
set -x
set -e

source /common.sh

apt-get update

# Install Go
curl -o go.tar.gz https://golang.org/dl/go1.15.2.linux-armv6l.tar.gz
tar -C /usr/local -xzf go.tar.gz
echo 'export PATH=$PATH:/usr/local/go/bin' > /etc/profile

apt-get clean
apt-get autoremove -y
